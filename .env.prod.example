# Example production environment variables.
# Copy to `.env` in the repository root before deploying:
#   cp .env.prod.example .env

POSTGRES_USER=raznaia
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=raznaia

SECRET_KEY=change-me-long-random-secret
CORS_ORIGINS=https://tms.example.com

HTTP_PORT=80
HTTPS_PORT=443
